/**
 *
 * @category URL
 */
export declare function isHttp(url: string): boolean;
/**
 * @category URL
 */
export declare function isUrl(url: string): boolean;
/**
 * @category URL
 */
export declare function isAbsoluteUrl(url: string): boolean;
/**
 * combines urls
 * @category URL
 * @example
 * ```ts
 * combineURLs('http://example.com', 'foo', 'bar') // => http://example.com/foo/bar
 * combineURLs('//example.com', '/foo') // => //example.com/foo
 * combineURLs('/foo', 'bar', 'index.html') // => /foo/bar/index.html
 * ```
 */
export declare function combineURLs(baseUrl: string, ...urls: string[]): string;
/**
 * @category URL
 * @example
 * ```ts
 * parseProtocol('http://example.com') // => http
 * parseProtocol('mailto:user@example.com') // => mailto
 * ```
 */
export declare function parseProtocol(url: string): string;
